# Dependencies
node_modules/

# Build artifacts (keep main.js for releases)
# main.js is the compiled plugin - we include it for easy installation

# IDE & OS
.DS_Store
Thumbs.db
.vscode/
.idea/

# Obsidian plugin data (user-specific)
data.json

# Agent internal state (never publish)
.agent/

# Debug
*.log
